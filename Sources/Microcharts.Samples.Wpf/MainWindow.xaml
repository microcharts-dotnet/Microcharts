<Window x:Class="Microcharts.Samples.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Microcharts.Samples.Wpf"
        xmlns:microcharts="clr-namespace:Microcharts.Wpf;assembly=Microcharts.Wpf"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <microcharts:ChartView Grid.Row="0" x:Name="chart1" />
        <microcharts:ChartView Grid.Row="1" x:Name="chart2" />
        <microcharts:ChartView Grid.Row="2" x:Name="chart3" />
        <microcharts:ChartView Grid.Row="3" x:Name="chart4" />

        <microcharts:ChartView Grid.Row="0" Grid.Column="1" x:Name="chart5" />
        <microcharts:ChartView Grid.Row="1" Grid.Column="1" x:Name="chart6" />
    </Grid>
</Window>
